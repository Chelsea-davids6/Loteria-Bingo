const { ICONS, GRID_SIZE } = require('./constants');

const shuffleArray = (array) => {
  const shuffled = [...array];
  for (let i = shuffled.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
  }
  return shuffled;
}

function generatePlayerCard() {
  const shuffled = shuffleArray(ICONS)
  const totalCards = GRID_SIZE * GRID_SIZE;

  const cardIcons = shuffled.slice(0, totalCards);
  console.log("Card icons:", cardIcons); 
  

  return {
    icons: cardIcons
  };

}

// console.log("Testing shuffle:");
// console.log(shuffleArray(ICONS).slice(0, 5));

module.exports = {
  shuffleArray,
  generatePlayerCard
};